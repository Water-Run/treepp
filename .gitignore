# ------------------------------------------------------------
# Zig
# ------------------------------------------------------------
# Zig build output directory (default)
zig-out/
# Zig local cache (project-local)
.zig-cache/
# Zig global cache (if accidentally created inside repo)
zig-cache/

# Common build artifacts (if you output elsewhere)
build/
dist/
out/

# Optional: if you generate docs locally
docs/_build/
docgen/

# ------------------------------------------------------------
# VS Code
# ------------------------------------------------------------
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json

# If you prefer NOT to commit any workspace settings, comment the 4 lines above
# and just keep:
# .vscode/

# ------------------------------------------------------------
# OS / Editor noise
# ------------------------------------------------------------
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini

# Windows / macOS metadata
$RECYCLE.BIN/
.AppleDouble
.LSOverride

# ------------------------------------------------------------
# Logs / temp
# ------------------------------------------------------------
*.log
*.tmp
*.temp
*.swp
*.swo
*.bak
*.old

# ------------------------------------------------------------
# Tooling caches
# ------------------------------------------------------------
# clangd / ccls (sometimes used when Zig emits compile_commands.json)
.cache/
compile_commands.json

# ------------------------------------------------------------
# Optional: Coverage / profiling
# ------------------------------------------------------------
*.profraw
*.profdata
*.gcda
*.gcno
*.gcov

# ------------------------------------------------------------
# Optional: Packaging artifacts
# ------------------------------------------------------------
*.zip
*.tar
*.tar.gz
*.tgz
*.7z

# Windows debug/link artifacts
*.pdb
*.ilk
*.obj

# Zig language server cache
.zls/
